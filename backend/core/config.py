# Danh sách các model được hỗ trợ, xếp theo thứ tự ưu tiên
# Model đầu tiên sẽ được thử trước. Nếu hết hạn ngạch, hệ thống sẽ tự động
# chuyển sang model tiếp theo trong danh sách.

AVAILABLE_MODELS = [
    "gemini-1.5-flash", # Ưu tiên 1
    "gemini-1.5-pro",   # Ưu tiên 2 (dự phòng)
    # Bạn có thể thêm các model khác vào đây
]
